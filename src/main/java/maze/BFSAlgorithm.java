package maze;

import java.util.HashMap;
import java.util.List;


public class BFSAlgorithm {
    public String[][] solveMazeWithBFS(String[][] maze) {
        
    }
    
    public HashMap<Integer, List<Integer>> generateGraph(String[][] maze) {
         HashMap<String, List<Integer>> graph = new HashMap<String, List<Integer>>();
         int k = 0;
         for(int i = 0;i < maze[0].length; i++) {
            for(int j = 0;j < maze.length; j++) {
                
                graph.put(k, );
                k++;
            }
         }
    }
}
